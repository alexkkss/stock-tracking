"""
测试运行配置
"""
import pytest


def pytest_configure(config):
    """配置pytest"""
    config.addinivalue_line(
        "markers", "asyncio: marks tests as async"
    )
    

# 测试覆盖率配置（可选）
# 运行: pytest --cov=app tests/
# 生成报告: pytest --cov=app --cov-report=html tests/
